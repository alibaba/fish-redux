name: test_widgets
description: widgets for redux test...

environment:
    sdk: '>=2.0.0-dev.28.0 <3.0.0'

dependencies:
    flutter:
        sdk: flutter
    fish_redux:
        path: ../../

dev_dependencies:
    flutter_driver:
        sdk: flutter
    flutter_test:
        sdk: flutter
